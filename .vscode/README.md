> **💡 Developer Note:**
> To build and compile the active file, press **`Ctrl` + `Shift` + `B`**. The repository is configured to handle Git pulls automatically when the workspace is opened.
> **`ctrl` + `shift` + `i`** to run prettier.
