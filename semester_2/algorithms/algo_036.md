# algo_036

### Problem Statement

> Write an algorithm to check if the queue is empty (Circular Queue)

## Algorithm
```
procedure isEmpty(front)
begin  
    if(front = -1)
        return true;
    else
        return false;
    endif
end procedure
```